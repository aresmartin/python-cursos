x = input( "Introduzca numero: " )
num = eval( x )
print( "El numero es {0:.2f}, y su doble es {1:.2f}".format( num, num * 2 ) )
# Online Python compiler (interpreter) to run Python online.
# Write Python 3 code in this online editor and run it.
import json

def fnc():
    print("hola")
    
class Avion:
    def __init__(cls, matricula):
        cls.matriz = matricula

class Coche:
    def __init__(cls, matricula):
        cls.matricula_ = matricula
        cls.matricula = matricula + 10
        cls.mat = matricula + 20
        
    def on(self):
        print(str(self.matricula_) + " encendido")

if __name__ == "__main__"
x = input( "calculadora: " )
    
    print(eval( x ))
    #print( "El numero es {0:.2f}, y su doble es {1:.2f}".format(num, num * 2 ) )
    c = Coche(10)
    a = Avion(200)
    #print(dir(c))
    #print(c.__class__)
    #print(c.__sizeof__())
    numer = eval("print(1)")
    print(getattr(c, "matricula_"))
    
    for elem in dir(a):
        if "mat" in elem:
            print(getattr(a, elem))
    #print(json.dumps(c.__dict__, indent = 2))

# barra bajas
    ## meta metodos -> introspeccion
# barra bajas
    ## meta metodos -> introspeccion
    ## barra baja al final o principio para indicar la intencion de atributo privado
    
# self
    ## primera variable de cada metodo es al propia instancia de la clase
# no hay main
    ## si lo hay
# tipado
    ## tipado flexible -> no hace falta indicar el tipo a las variables
    ## tipado dinamico -> las variables pueden cambair de tipo despues de instanciadas
    
# introspeccion
